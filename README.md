How to Run the project

1. Navigate to actual_project-> and run pip install -r requirements.txt

2. Navigate to react->backend-> and run uvicorn main:app --reload
app will start at http://127.0.0.1:8000

3. Navigate to react->frontend->and run
    1. npm i
    2. npm run dev and the server will start at http://localhost:5173/